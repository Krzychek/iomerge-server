description = ''

configurations {
	provided
}

sourceSets {
	main {
		compileClasspath += configurations.provided
	}
}

dependencies {
	compile project(':data-model')
	provided group: 'org.springframework', name: 'spring-context', version: '4.3.1.RELEASE'
}
